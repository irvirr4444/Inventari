{
  "name": "inventari",
  "private": true,
  "workspaces": [
    "backend",
    "frontend",
    "packages/shared"
  ],
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,green \"npm -w backend run dev\" \"npm -w frontend run dev\"",
    "build": "npm -w backend run build && npm -w frontend run build",
    "lint": "npm -w backend run lint && npm -w frontend run lint",
    "test": "npm -w @inventari/shared run test && npm -w backend run test"
  },
  "devDependencies": {
    "concurrently": "^9.2.0"
  }
}

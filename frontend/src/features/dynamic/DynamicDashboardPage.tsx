import { DashboardPage } from '../../pages/DashboardPage'

/** Dynamic users with legacy-style layout reuse unchanged dashboard until N-location panels expand. */
export function DynamicDashboardPage() {
  return <DashboardPage />
}

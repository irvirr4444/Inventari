{
  "name": "@inventari/android-shell",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "cap:sync": "npx cap sync android",
    "cap:open": "npx cap open android",
    "cap:add:android": "npx cap add android",
    "cap:icons": "python3 scripts/prepare-icon-assets.py && npx @capacitor/assets generate --android --androidProject android --iconBackgroundColor '#ffffff' --splashBackgroundColor '#071528' && python3 scripts/patch-android-adaptive-icon.py && python3 scripts/patch-legacy-launcher-icons.py"
  },
  "dependencies": {
    "@capacitor-community/safe-area": "^7.0.0",
    "@capacitor/android": "^7.4.2",
    "@capacitor/core": "^7.4.2",
    "@capgo/capacitor-social-login": "^7.20.0"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5",
    "@capacitor/cli": "^7.4.2"
  }
}

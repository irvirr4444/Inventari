SUPABASE_URL=
SUPABASE_SERVICE_KEY=

PORT=3001
CORS_ORIGIN=http://localhost:5173
# Production with Android APK: comma-separated, e.g.:
# CORS_ORIGIN=https://your-frontend.onrender.com,https://localhost

# Legacy admin (one-time seed: npm run seed:legacy-user -w backend)
login_email=
login_password=

SESSION_SECRET=replace-with-at-least-32-random-characters
# Optional: session lifetime in seconds (default ~400 days). Example 8h: 28800
# SESSION_MAX_AGE_SECONDS=

# Optional
GOOGLE_CLIENT_ID=
DATABASE_URL=
